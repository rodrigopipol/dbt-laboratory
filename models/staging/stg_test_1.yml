version: 2

models:
  - name: stg_test_1
    description: Tabla staging de ejemplo
    columns: 
      - name: id
        description: '{{ doc ("ejemplo_test") }}'
        tests:
          - unique
          - not_null
          - accepted_values:
              values:
                - "45"