version: 2

sources:
      - name: src_external_auditsa_cl
        description: Tabla externa creada en DBT
        external:
          location: 'gs://apt-advantage-312018-staging/DW - Inputs/Auditsa/Chile/Santiago/auditsa_cl_santiago_off_tv.csv'
          options:
            formato: csv
            skipLeadingRows: 1
            fielDelimiter: ','
            allowJaggedRows: true